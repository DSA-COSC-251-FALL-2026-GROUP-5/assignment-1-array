public class OrderedArray extends GenericArray {
  OrderedArray(int size) {
    super(size);
  }

  @Override
  void insert(int x) {
    // TODO: insert by doing doing binary search to find the mid-point, and then
    // inserting it here.
  }

  @Override
  int find(int x) {
    // TODO: find by doing binary search.
    return 0;
  }
}
